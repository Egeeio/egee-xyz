{
  "name": "egee-xyz",
  "version": "1.0.0",
  "description": "egee's personal website",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "./node_modules/.bin/http-server ./public",
    "build": "./node_modules/.bin/pug ./src --out ./public && sass --no-source-map ./src/site.sass:public/assets/style/site.css --style compressed",
    "start": "npm run build && npm run serve"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/egee-irl/egee-xyz.git"
  },
  "author": "egee",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/egee-irl/egee-xyz/issues"
  },
  "homepage": "https://github.com/egee-irl/egee-xyz#readme",
  "devDependencies": {
    "http-server": "^0.12.3",
    "node-sass": "^4.14.1",
    "pug-cli": "^1.0.0-alpha6"
  }
}
